package com.easycar.order_service.domain;

// NOTE: Copied from product service. Should be replaced with shared module later.
public enum ProductCategory {
    SEDAN,
    SUV,
    HATCHBACK,
    VAN
}
